import PlaceholderPage from '../components/PlaceholderPage.jsx';

export default function StartAProjectPage() {
  return (
    <PlaceholderPage
      eyebrow="Inquiry"
      title="Start a Project"
      body="Route placeholder only. Inquiry workflow, fields, integrations, and form design are deferred for a later milestone."
    />
  );
}
